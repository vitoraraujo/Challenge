<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<% if logged_in? %>
	<% if current_user.admin? || current_user?(@user) %>
		<%= button_to 'Destroy', @user, method: :delete, data: { confirm: 'Are you sure?' } %></td> |
	<% end %>
<% end %>
<%= link_to 'Back', users_path %>
